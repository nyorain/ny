//create a window using a different library

auto someExternalWindowObject = ...

//just create a gui and connect the interfaces over onDraw
ny::Gui gui{};

//...

auto& dc = getNyDrawContexForExternalWindow(someExternalWindowObject);
gui.draw(dc);

//create a ny::Window for the external window
auto handle = someExternalWindowObject.getNativeHandle();
ny::Window window(handle);

ny::Gui
